public interface Interactable {
    public static int var = 0;
    // varible d'instance  / variable de classe (static) unique pour toutes les instances ?
    void enabled();


    void interagir();
}
